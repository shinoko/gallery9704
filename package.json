{
  "name": "gallery9704",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "export:static": "node server.js --export-static",
    "build:data": "node scripts/build-static-data.js && node scripts/build-static-data.js official-metadata.json js/official-data.js --data-var officialGalleryData --facets-var officialGalleryFacets",
    "clean:data": "node scripts/clean-metadata.js",
    "clean:data:dry": "node scripts/clean-metadata.js --dry-run"
  }
}
